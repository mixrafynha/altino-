import Image from "next/image";import Link from "next/link";import { notFound } from "next/navigation";import { products } from "@/data/products";import { money } from "@/lib/format";
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=products.find(x=>x.slug===slug);if(!p)return notFound();return <main className="safe-bottom mx-auto grid max-w-7xl gap-10 px-4 py-14 md:grid-cols-2 md:px-8"><div className="relative aspect-square overflow-hidden rounded-[44px] rose-shadow"><Image src={p.image} alt={p.name} fill className="object-cover"/></div><section><p className="font-bold uppercase tracking-[.24em] text-[#D59B45]">Altino Construções</p><h1 className="mt-3 text-6xl font-semibold">{p.name}</h1><p className="font-serif-soft mt-4 text-2xl text-[#68625B]">{p.subtitle}</p><p className="mt-6 text-lg leading-8 text-[#68625B]">{p.description}</p><div className="mt-6 flex flex-wrap gap-2">{p.badges.map(b=><span key={b} className="rounded-full bg-[#E6DDD0] px-4 py-2 text-sm font-bold">{b}</span>)}</div><p className="mt-8 text-2xl font-bold">à partir de {money(p.priceCents)}</p><div className="mt-8 flex flex-wrap gap-3"><Link href={`/designer?product=${p.slug}`} className="rounded-full bg-[#23262B] px-7 py-4 font-bold text-[#FFFCF7]">Demander un devis</Link><Link href="/shop" className="rounded-full bg-white px-7 py-4 font-bold text-[#23262B] rose-shadow">Retour services</Link></div></section></main>}

import Image from "next/image";import Link from "next/link";import { money } from "@/lib/format";import type { Product } from "@/data/products";
export function ProductCard({p}:{p:Product}){return <Link href={`/product/${p.slug}`} className="group block overflow-hidden rounded-[32px] bg-[#FFFCF7] rose-shadow"><div className="relative aspect-[4/5] overflow-hidden"><Image src={p.image} alt={p.name} fill sizes="(max-width:768px) 90vw, 33vw" className="object-cover transition duration-700 group-hover:scale-105"/><div className="absolute left-4 top-4 flex gap-2">{p.badges.slice(0,2).map(b=><span key={b} className="rounded-full bg-[#FFFCF7]/90 px-3 py-1 text-xs font-bold text-[#A66B3F]">{b}</span>)}</div></div><div className="p-5"><p className="font-serif-soft text-xl text-[#68625B]">{p.subtitle}</p><h3 className="mt-1 text-2xl font-semibold text-[#23262B]">{p.name}</h3><div className="mt-4 flex items-center justify-between"><span className="font-bold">à partir de {money(p.priceCents)}</span><span className="rounded-full bg-[#E6DDD0] px-4 py-2 text-sm font-bold">Demander devis</span></div></div></Link>}
